import styled from "styled-components"

const ImageView = () => {

    return (

        <>
            <Background>


            </Background>

        </>
    )
}

const Background = styled.div`
  position: fixed;
  top: 0;
  left: 0;
  right: 0;
  bottom: 0;

  background: black;
  z-index: 1;
`;

export default ImageView;
